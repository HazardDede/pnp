FROM arm32v7/python:3.6-stretch

ENV WORKDIR=/tmp/pnp
ENV CONFDIR=/config

RUN apt-get update -yy && \
    apt-get install -yy git

COPY . ${WORKDIR}

RUN cd ${WORKDIR} && \
    pip3 install --process-dependency-links .[dht]

VOLUME /config

CMD ["pnp", "/config/config.json"]