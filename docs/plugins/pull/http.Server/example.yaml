api:
  port: 9999
tasks:
  - name: rest
    pull:
      plugin: pnp.plugins.pull.http.Server
      args:
        prefix_path: callme
        allowed_methods:
          - GET
          - POST
    push:
      plugin: pnp.plugins.push.simple.Echo
